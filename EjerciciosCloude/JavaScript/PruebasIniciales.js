

const definicion = (n) => {
    let dinero = parseFloat(prompt(`Dinero de ${n}`));
    let helado, precio;

    if (dinero === 10) { helado = "Helado"; precio = 1; }
}

console.log(helado); // ¿qué pasa aquí?















